# Request-Smuggling-Journey-From-Payloads-to-patched
 I crafted payloads, tested multiple endpoints, and analyzed raw responses only to discover that the target server accepted malformed requests but ignored embedded payloads. Instead of leaking usernames or passwords, the server consistently returned the same design HTML, proving that the vulnerability was patched and not exploitable.
